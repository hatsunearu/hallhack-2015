function p1()
    c = str2double(input('num?', 's'));
    disp(factorial(c));
     c = str2double(input('num?', 's'));
    disp(factorial(c));
    c = str2double(input('num?', 's'));
    disp(factorial(c));
    
end 